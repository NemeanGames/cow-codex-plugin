[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "cow-plugin-runtime"
version = "2.0.0.post8"
description = "Public subset of the COW runtime, distributed inside the plugin"
requires-python = ">=3.10"
license = {file = "LICENSE"}
dependencies = []
[tool.setuptools.packages.find]
where = ["src", "third_party/cityqa-deterministic/src"]
