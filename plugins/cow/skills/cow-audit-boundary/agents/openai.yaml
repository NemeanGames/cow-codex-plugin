interface:
  display_name: "COW Audit Boundary"
  short_description: "Keep producers, auditors, and evidence separate"
  default_prompt: "Use $cow-audit-boundary to review this candidate without collapsing producer assertions into verification."
policy:
  allow_implicit_invocation: true
